{
  "name": "@ccd/db",
  "version": "0.0.0",
  "description": "SQLite storage for coverage sets, aggregate runs, practical runs and the per-tx index.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12"
  }
}
